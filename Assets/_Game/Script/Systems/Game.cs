using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static EmotionSystem;

/*public class Game
{
    // Definiere einen Spieler
    public Player_Base Player { get; set; }

    // Konstruktor
    public Game()
    {
        Player = new Player_Base();
    }

    // Füge einer Emotion einen Wert hinzu
    public void AddEmotion(EmotionType type, float value)
    {
        Player.AddEmotion(new EmotionSystem(type, value));
    }

    // Berechne den Gesamtwert aller Emotionen
    public float GetTotalEmotionValue()
    {
        return Player.GetTotalEmotionValue();
    }

    // Prüfe, ob der Spieler sich in ein Monster verwandelt
    public bool IsTransformingIntoMonster()
    {
        return Player.IsTransformingIntoMonster();
    }
}*/
